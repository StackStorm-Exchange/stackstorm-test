name: Release

on:
  push:
    branches:
      # the default branch
      - aaa

permissions:
  contents: write
  metadata: write

jobs:
  tag_release:
    name: Tag Release
    uses: StackStorm-Exchange/ci/.github/workflows/pack-tag_release.yaml@gha
  repo_metadata:
    name: Repo Metadata
    uses: StackStorm-Exchange/ci/.github/workflows/pack-repo_meta.yaml@gha
